<div id="vasijas">
	
	
</div>
Son las <%= Time.now.strftime('%H:%M a %d, %b')%><br />

<hr>
<!--
<%= calendar({:year => Time.now.year, :month => Time.now.month, :table_class =>
"calendar",:first_day_of_week => 1}  ) do |d|
	@dia=d
  if @dates.include?(d.strftime('%d-%m-%Y'))
    [link_to_function(d.mday, {:class => "specialDay"}) do |page|
	page[:citas].replace_html :partial => 'cita',:collection => @citas
	page[:citas].visual_effect 'highlight', :duration => 1.3
	end]
  else  
    [d.mday, {:class => "day"}]
  end
end %>
<div id="box_citas" >
	<h4>Citas:</h4>

	<div id ="citas"></div>

</div>
!-->
<br />

<br />
<br />
<div>
<%# link_to_prototype_window('Link', 'Hola') %>
<%# link_to_function "link",'Dialog.confirm({url:"Hola", options: {method: "get"}}, 
                     {top: 10, width:250, className: "alphacube", okLabel: "Yes", cancelLabel:"No"});' %>
</div>

<div id="primario"> 

     <div id='principal'> 
          <h1>Citas para hoy</h1>
			<%= render :partial => 'lab/recepcion', :collection => @consultas_hoy   %>
     </div> 

<div id='barra-der'> 
     <div class="barra-citas"> 
         <h1>Citas para confirmar hoy </h1> 
			<%= render :partial => 'lab/recepcion_confirma', :collection => @confirma_hoy   %>
    </div> 
     <div class="barra-citas"> 
        <h1>Citas sin confirmar</h1> 
			<%= render :partial => 'lab/recepcion_sin_conf', :collection => @sin_confirma   %>
    </div> 
</div>


