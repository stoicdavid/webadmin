<%= calendar({:year => Time.now.year, :month => 1.month.ago(Time.now.beginning_of_month).month,
	 :table_class => "calendar_helper"}) do |d|
  if @citas.include?(d)
    [d.mday, {:class => "specialDay"}]
  else  
    [d.mday, {:class => "day"}]
  end
end %>